/** Starred view (Phase 13) copy — its own overflow file; see `strings.en.epg.ts` for why these live outside `strings.en.ts`. */
export const enStarred = {
    starred: {
        /** Prefixed to the channel name in each row's tooltip. */
        playTitle: 'Play',
        unstar: 'Remove from Starred',
        radioBadge: 'Radio',
    },
};
